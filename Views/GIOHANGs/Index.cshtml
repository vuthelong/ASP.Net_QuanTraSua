@model IEnumerable<ASP.NET_QuanTraSua.Models.GIOHANG>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DONHANG.PhuongThucThanhToan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DONHANG.PhuongThucThanhToan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaGH }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaGH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaGH })
        </td>
    </tr>
}

</table>
