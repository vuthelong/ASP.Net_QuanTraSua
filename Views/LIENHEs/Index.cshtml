@model IEnumerable<ASP.NET_QuanTraSua.Models.LIENHE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChiLH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiaChiLH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLH }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLH })
        </td>
    </tr>
}

</table>
